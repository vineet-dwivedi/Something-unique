{"message":"In the current sandbox workspace, find every file that imports `react-scroll` and remove that dependency completely. Start with `src/sections/Footer.jsx`, replace the `Link` usage with a native `<a href>` or button-based equivalent, and keep the current design and behavior intact. If any other file imports `react-scroll`, fix it too. After updating, re-read the changed files and confirm none of them still reference `react-scroll`.","projectId":"019fbca6-5982-730c-96e6-98ab0b591b78"}